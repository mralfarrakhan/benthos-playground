apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: benthos-playground
spec:
  type: LoadBalancer # Exposes the UI outside of the cluster for local host access
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: kafka-ui