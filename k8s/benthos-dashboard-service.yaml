apiVersion: v1
kind: Service
metadata:
  name: benthos-dashboard
  namespace: benthos-playground
spec:
  ports:
  - port: 3000
    name: http
  type: LoadBalancer # Colima handles LoadBalancers locally!
  selector:
    app: benthos-dashboard